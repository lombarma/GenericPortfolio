import {Item} from './item.type';

export interface Data {
  items: Item[];
  name: string;
  descriptionAboutYou: string;
  phrases: string[];
  linkGithub: string;
  linkLinkedIn: string;
  linkEmail: string;
}
